{
  "name": "photography-portfolio-site",
  "version": "1.0.0",
  "description": "photography portfolio site ",
  "author": "Tom Armstrong",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "DEPLOY_ENV=STATIC nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --allow-unclean --cleanup"
  },
  "dependencies": {
    "kenticocloud-nuxt-module": "^2.2.1",
    "nuxt": "^2.0.0",
    "rxjs": "^6.5.3"
  },
  "devDependencies": {
    "@nuxtjs/style-resources": "^1.0.0",
    "node-sass": "^4.13.0",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.0"
  }
}
